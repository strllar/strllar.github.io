markdown: kramdown
kramdown:
  auto_ids: true
  input: GFM
brand-title: STRLLAR Lab
brand-tagline: Toward Pervasive Exchange

defaults:
  -
    scope:
      path: ""      # empty string for all files
      type: posts   # limit to posts
    values:
      is_post: true # automatically set is_post=true for all posts
